# Python 3.10.11
# PyTorch + CUDA 11.8 are already installed in your environment.
# If needed, install torch separately from the official PyTorch index.

numpy
scikit-learn
tqdm
einops
pandas
openpyxl
